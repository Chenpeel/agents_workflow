# {TARGET} 当前事实与推进进度（current.md）

> **文档定位**：本文件是 {TARGET} 任务的**当前事实**与**推进进度**记录：现状快照（已落地 / 未落地）+ 相对 `plan.md` 的整体完成度，是蓝本与现实的对照基准。
> **读者**：正在推进该任务的 Agent（每轮开工前先读本文件确认基线与进度）、评审者；蓝本发生变更时的变更记录处。
> **更新规则**：每轮实现、提交后同步更新；只记录事实，不得修改蓝本内容；蓝本被修改时在此记录变更说明。
> **配套文档**：`plan.md`（实施方案蓝本）· `responsibilities.md`（职责蓝本）。

> **基于 `./plan.md` 的全局进度：0%**
>
> `[░░░░░░░░░░░░░░░░░░░░]`
>
> 进度说明：每完成一个 `plan.md` 中的阶段，同步刷新上方百分比与进度条（每 5% 一格，共 20 格）。

> **工作环境**：本任务绑定 worktree：`$WORKTREE_PATH/_{TARGET}`
> （`WORKTREE_PATH` 为环境变量，已在 `~/.zshrc` 中 export）。
> 所有实现、构建、提交均在该 worktree 内进行；开工前先确认当前所在目录正确。

## 1. 文档定位

> 一句话说明：本文件记录什么（当前任务"实际是什么样、推进到哪"）。

## 2. 当前事实总览

> 用表格或清单概述：已完成、进行中、未开始的主要事项。

## 3. 当前事实细节

> 逐项写清事实：已落地的代码/接口/行为，标注对应 `plan.md` / `responsibilities.md` 的条目。

## 4. 当前尚未落地

> 明确列出尚未实现或偏离蓝本的事项，附原因。

## 5. 推进进度（本轮）

> 记录本轮推进：本轮目标（对应 `plan.md` 哪个阶段）→ 执行顺序 → 完成度变化（X% → Y%）。
> 每轮完成后刷新上方全局进度；本节保留简短时间线（每轮一行），不写长文。

## 6. 基于当前事实到长期规划

> 从当前事实出发，下一阶段要做什么、与蓝本的差距如何收敛。

## 7. 简短结论

> 一句话结论：当前状态与蓝本是否一致、完成度多少、下一步动作。
